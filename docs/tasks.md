# Throw In - 開発タスク管理（To-Do リスト）

今後の開発ステップをブレイクダウンしたタスクリストです。
マークダウン標準のチェックボックス `[ ]` (未着手) と `[x]` (完了) を使用してシンプルに進捗を管理します。
進行中のものはタスク名の末尾に `(WIP)` を付与して明示します。

## フェーズ 1: 基盤環境と認証のセットアップ (Supabase)
- [x] 1.1. Supabaseプロジェクトの初期設定
  - [x] プロジェクト作成、環境変数設定
  - [x] クライアント初期化ユーティリティの実装
- [x] 1.2. データベーススキーマ構築 (PostgreSQL)
  - [x] テーブル定義 (users, clips, collections, clip_tags, history)
  - [x] RLS (Row Level Security) ポリシーの設定
- [x] 1.3. ユーザー認証機能の実装 (Supabase Auth)
  - [x] ログイン・サインアップ画面の作成
  - [x] 認証状態監視フック/プロバイダの実装
  - [x] 未ログイン時のルーティングアクセス制御
  - [x] アカウント退会・全データ削除機能の実装

## フェーズ 2: コアCRUDの実装とインメモリ状態からの移行
- [x] 2.1. クリップデータ取得機能
  - [x] DBからのデータフェッチ (フィルタリング、タグ対応含む)
  - [x] 閲覧履歴（History）のトラッキングと一覧表示
- [x] 2.2. クリップの新規登録
  - [x] フォームからのDB書き込み
  - [x] 画像/PDFのアップロード機能 (Supabase Storage / 容量・形式バリデーション)
- [x] 2.3. クリップ情報の更新・削除
  - [x] ブックマーク、既読/未読状態のトグル保存
  - [x] メモの編集保存、クリップのアーカイブ・削除
- [x] 2.4. コレクション機能
  - [x] コレクション作成、クリップの紐付け機能

## フェーズ 3: データパイプラインとAI連携 (コア機能)
- [x] 3.1. Webサイトスクレイピング
  - [x] URLからのテキスト、OGP、タイトル抽出用APIの構築
  - [x] SSRFガード（無効IP/内網IPへのリクエスト事前ブロック）
- [x] 3.2. テキスト・データ抽出
  - [x] アップロードされたPDF・画像からのOCR/テキスト抽出
  - [x] 動画（YouTube等）からの字幕（トランスクリプト）抽出
- [x] 3.3. OpenAI API連携 (GPT-4o利用) 
  - [x] 設定画面等でのユーザー固有OpenAI APIキー入力・保存UIの実装
  - [x] ユーザーAPIキーと固定化されたシステムプロンプトによるテキスト要約・タグ付け処理
- [x] 3.4. 非同期ジョブとUIステータス連携
  - [x] AI処理のバックグラウンド実行機能
  - [x] エラーハンドリング・自動リトライ機構
  - [x] クライアントUIへの処理ステータス反映 (処理中、完了など)

## フェーズ 4: 高度な検索とUX向上・運用機能
- [x] 4.1. 検索エンジンの統合
  - [x] 全文検索の実装 (フロントエンド側の簡易フィルタ・文字検索対応完了)
  - [x] pgvector等を利用したセマンティック（類似度）検索
- [x] 4.2. パフォーマンスと信頼性
  - [x] リスト表示、詳細UI（Reader View）機能実装
  - [x] リストのページネーション (無限スクロール等)
  - [x] 画像最適化（Next.js Image/CDN活用）
  - [x] オフラインサポート（PWAキャッシュ/IndexedDB等による耐障害性）
- [x] 4.3. 発展機能・管理
  - [x] キュレーション分析レポート機能（準備モック実装）
  - [x] PWA Web Share Target対応
  - [x] アドミンダッシュボード構築 (ジョブエラー監視、利用統計)

## フェーズ 5: モック・未実装機能の完成

### 5.1. バグ修正（高優先）
- [x] `app/clip/[id]/page.tsx:189` — 「元記事を開く」リンクが `clip.id` を使っており外部URLに遷移できない → `clip.url` に修正

### 5.2. ページ実装
- [x] `app/bookmarks/page.tsx` — ブックマークページが空表示のみ。`useClipStore` から `isBookmarked` でフィルタして一覧表示する
- [x] `app/search/page.tsx` — 検索ページ全体がモック
  - [x] ハードコードされたダミー結果2件を削除
  - [x] フィルター（期間・ソート）をstateに接続
  - [x] 人気タグのクリックで検索クエリにセット
  - [x] ページネーション実装

### 5.3. 機能完成
- [x] `app/add/page.tsx:440-446` — タグ追加UIが `'New'` 固定文字列を追加するだけ → 入力テキストを実際にタグとして追加する
- [x] `app/settings/page.tsx:36-42` — アカウント削除がサインアウトにフォールバック → `POST /api/delete-account` で全データ削除 + `auth.admin.deleteUser` 実装
- [x] `app/settings/page.tsx:141` — PWA「インストール」ボタンに `onClick` なし → `beforeinstallprompt` イベントで実装

### 5.4. 管理機能（低優先）
- [x] `app/admin/page.tsx:97-113` — 「ジョブ管理」「公開カードレビュー」「ユーザー管理」カードにナビゲーション先を実装（スタブページ作成）

### 5.5. DBスキーマ追加
- [x] `clips` テーブルに `is_archived boolean default false` カラム追加 → `supabase/03_add_is_archived.sql` + `updateClip` / `fetchClips` 対応

---

## フェーズ 6: 正式リリース版への移行体制
- [ ] 6.1. AI実行アーキテクチャの切り替え
  - [ ] ユーザー固有のAPIキー入力UIの撤廃
  - [ ] 各ユーザーの利用プラン（制限付き）に応じたAdmin APIキー利用への切り替え
